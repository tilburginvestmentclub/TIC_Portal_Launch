# --- Python & Environment ---
__pycache__/
*.pyc
venv/
.venv/

# --- Secrets ---
# Only ignore the secrets file, not the whole folder (in case you add config.toml later)
.streamlit/secrets.toml
secrets.toml

# --- Local Data (VPS will generate its own) ---
# We ignore these so we don't spam GitHub with data updates
database_snapshot.json
market_snapshot.json

# --- Logs & System Junk ---
*.log
.DS_Store